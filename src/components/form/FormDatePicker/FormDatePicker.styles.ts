import { StyleSheet } from 'react-native';

const formDatePickerStyles = StyleSheet.create({
	input: {
		borderRadius: 5,
	},
	bottomMargin: {
		marginBottom: 20,
	},
});

export default formDatePickerStyles;
